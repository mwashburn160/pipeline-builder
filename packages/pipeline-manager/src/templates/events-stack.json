{
  "AWSTemplateFormatVersion": "2010-09-09",
  "Description": "Pipeline Builder event ingestion — EventBridge rule, SQS queue, and Lambda that resolves each pipeline's pb.pipeline-id tag and forwards CodePipeline events to the reporting service API. Lambda code is deployed separately via pipeline-manager infra setup-events.",
  "Parameters": {
    "PlatformBaseUrl": {
      "Type": "String",
      "Description": "Base URL of the platform (e.g. https://app.example.com)"
    },
    "PlatformSecretName": {
      "Type": "String",
      "Description": "Secrets Manager secret name containing the platform JWT token (e.g. pipeline-builder/{orgId}/platform)"
    },
    "DoraEnabled": {
      "Type": "String",
      "Default": "false",
      "AllowedValues": ["false", "true"],
      "Description": "When true, the ingestion Lambda resolves commit timestamps in-account for DORA lead time (extra SCM/secret cost). Enable only for orgs with the advanced_reporting add-on."
    }
  },
  "Resources": {
    "EventQueue": {
      "Type": "AWS::SQS::Queue",
      "Properties": {
        "QueueName": "pipeline-builder-events",
        "VisibilityTimeout": 300,
        "MessageRetentionPeriod": 1209600,
        "RedrivePolicy": {
          "deadLetterTargetArn": { "Fn::GetAtt": ["DeadLetterQueue", "Arn"] },
          "maxReceiveCount": 3
        }
      }
    },
    "DeadLetterQueue": {
      "Type": "AWS::SQS::Queue",
      "Properties": {
        "QueueName": "pipeline-builder-events-dlq",
        "MessageRetentionPeriod": 1209600
      }
    },
    "EventQueuePolicy": {
      "Type": "AWS::SQS::QueuePolicy",
      "Properties": {
        "Queues": [{ "Ref": "EventQueue" }],
        "PolicyDocument": {
          "Statement": [{
            "Effect": "Allow",
            "Principal": { "Service": "events.amazonaws.com" },
            "Action": "sqs:SendMessage",
            "Resource": { "Fn::GetAtt": ["EventQueue", "Arn"] }
          }]
        }
      }
    },
    "EventRule": {
      "Type": "AWS::Events::Rule",
      "Properties": {
        "Name": "pipeline-builder-codepipeline-events",
        "Description": "Capture CodePipeline state changes for reporting",
        "State": "ENABLED",
        "EventPattern": {
          "source": ["aws.codepipeline"],
          "detail-type": [
            "CodePipeline Pipeline Execution State Change",
            "CodePipeline Stage Execution State Change",
            "CodePipeline Action Execution State Change"
          ]
        },
        "Targets": [{
          "Id": "pipeline-events-queue",
          "Arn": { "Fn::GetAtt": ["EventQueue", "Arn"] }
        }]
      }
    },
    "LambdaRole": {
      "Type": "AWS::IAM::Role",
      "Properties": {
        "RoleName": "pipeline-builder-event-ingestion",
        "AssumeRolePolicyDocument": {
          "Version": "2012-10-17",
          "Statement": [{
            "Effect": "Allow",
            "Principal": { "Service": "lambda.amazonaws.com" },
            "Action": "sts:AssumeRole"
          }]
        },
        "ManagedPolicyArns": [
          "arn:aws:iam::aws:policy/service-role/AWSLambdaBasicExecutionRole"
        ],
        "Policies": [{
          "PolicyName": "event-ingestion",
          "PolicyDocument": {
            "Statement": [
              {
                "Effect": "Allow",
                "Action": ["sqs:ReceiveMessage", "sqs:DeleteMessage", "sqs:GetQueueAttributes"],
                "Resource": { "Fn::GetAtt": ["EventQueue", "Arn"] }
              },
              {
                "Effect": "Allow",
                "Action": ["sqs:StartMessageMoveTask", "sqs:ListMessageMoveTasks", "sqs:GetQueueAttributes"],
                "Resource": [
                  { "Fn::GetAtt": ["EventQueue", "Arn"] },
                  { "Fn::GetAtt": ["DeadLetterQueue", "Arn"] }
                ]
              },
              {
                "Effect": "Allow",
                "Action": "secretsmanager:GetSecretValue",
                "Resource": [
                  { "Fn::Sub": "arn:aws:secretsmanager:${AWS::Region}:${AWS::AccountId}:secret:pipeline-builder/*/platform-*" },
                  { "Fn::Sub": "arn:aws:secretsmanager:${AWS::Region}:${AWS::AccountId}:secret:pipeline-builder/*/reporting-ingest-*" },
                  { "Fn::Sub": "arn:aws:secretsmanager:${AWS::Region}:${AWS::AccountId}:secret:pipeline-builder/*/github-token-*" }
                ]
              },
              {
                "Effect": "Allow",
                "Action": "codepipeline:ListTagsForResource",
                "Resource": { "Fn::Sub": "arn:aws:codepipeline:*:${AWS::AccountId}:*" }
              },
              {
                "Effect": "Allow",
                "Action": "codecommit:GetCommit",
                "Resource": { "Fn::Sub": "arn:aws:codecommit:*:${AWS::AccountId}:*" }
              }
            ]
          }
        }]
      }
    },
    "EventHandler": {
      "Type": "AWS::Lambda::Function",
      "Properties": {
        "FunctionName": "pipeline-builder-event-ingestion",
        "Runtime": "nodejs24.x",
        "Handler": "index.handler",
        "Code": {
          "ZipFile": "exports.handler=async(event)=>{console.log('Placeholder handler — run pipeline-manager infra setup-events to deploy real code');return{statusCode:200,body:JSON.stringify({message:'hello',records:event.Records?.length||0})}}"
        },
        "Role": { "Fn::GetAtt": ["LambdaRole", "Arn"] },
        "Timeout": 60,
        "MemorySize": 256,
        "Environment": {
          "Variables": {
            "PLATFORM_BASE_URL": { "Ref": "PlatformBaseUrl" },
            "PLATFORM_SECRET_NAME": { "Ref": "PlatformSecretName" },
            "DORA_ENABLED": { "Ref": "DoraEnabled" }
          }
        }
      }
    },
    "EventSourceMapping": {
      "Type": "AWS::Lambda::EventSourceMapping",
      "Properties": {
        "EventSourceArn": { "Fn::GetAtt": ["EventQueue", "Arn"] },
        "FunctionName": { "Ref": "EventHandler" },
        "BatchSize": 10,
        "MaximumBatchingWindowInSeconds": 5,
        "Enabled": true
      }
    }
  },
  "Outputs": {
    "EventQueueArn": { "Value": { "Fn::GetAtt": ["EventQueue", "Arn"] } },
    "EventQueueUrl": { "Value": { "Ref": "EventQueue" } },
    "DeadLetterQueueUrl": { "Value": { "Ref": "DeadLetterQueue" } },
    "LambdaFunctionArn": { "Value": { "Fn::GetAtt": ["EventHandler", "Arn"] } },
    "EventRuleName": { "Value": { "Ref": "EventRule" } }
  }
}
