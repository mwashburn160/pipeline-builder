# Secrets
.env

# MongoDB replica-set keyfile — generated per-deploy by setup.sh/startup.sh
# (openssl rand). Must NOT be committed; rotate per environment.
# Not tracked in git — generated per-deploy by deploy/bin/mongo-keyfile.sh
# (pb_ensure_mongo_keyfile), called from startup.sh. A fresh checkout ships none.
mongodb-keyfile

# Generated certificates
certs/

# Registry auth
auth/

# Persistent data
data/
