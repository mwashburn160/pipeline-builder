apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

# EC2 overlay — base manifests use EC2 resource values by default.
# Only environment-specific overrides go here.

resources:
  - ../../../base
