# TLS certificates — generated by setup.sh
certs/

# Persistent data directories — host-side mount source
data/

# Environment file with secrets
.env

# MongoDB replica-set internal-auth keyfile — generated per-deploy by
# deploy/bin/mongo-keyfile.sh (pb_ensure_mongo_keyfile). A fresh checkout ships
# none. Must NOT be committed; rotate per environment.
mongodb-keyfile

# macOS
.DS_Store
