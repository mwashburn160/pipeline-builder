{
  "name": "block-latest-version",
  "description": "Plugin versions must not be the literal string 'latest' — pin to a specific semver for reproducible builds",
  "target": "plugin",
  "severity": "error",
  "field": "version",
  "operator": "neq",
  "value": "latest",
  "priority": 90,
  "scope": "published",
  "tags": ["security", "docker", "reproducibility"]
}
