# SpotBugs Plugin — Java Static Bug Analysis
# Used by CodeBuildStep in CodePipeline via pipeline-core Builder
#
# Thin layer over pipeline-jvm-base (one pinned Corretto JDK + Maven/Gradle).
# SpotBugs runs via the project's own Gradle/Maven build, so there is nothing
# to install here.

FROM pipeline-jvm-base:1.0

WORKDIR /app
CMD ["bash"]
