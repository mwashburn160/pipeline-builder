name: artillery
description: Artillery load testing plugin for running HTTP and WebSocket performance tests with configurable scenarios and reporting using AWS CDK CodeBuildStep
keywords:
  - load-test
  - performance
  - http
  - websocket
category: testing
version: 1.0.0
pluginType: CodeBuildStep
computeType: MEDIUM
timeout: 60
failureBehavior: fail
secrets: []
primaryOutputDirectory: artillery-reports
dockerfile: Dockerfile
installCommands:
  # Node + npm are baked into pipeline-node-base; the Artillery CLI is pinned + baked.
  - test -d artillery-reports || mkdir -p artillery-reports
  - artillery version
commands:
  - |
    set -e
    if [ -z "${ARTILLERY_CONFIG}" ]; then
      ARTILLERY_CONFIG=$(find . -name "artillery*.yml" -o -name "artillery*.yaml" -o -name "load-test*.yml" 2>/dev/null | head -1)
    fi
    if [ -z "${ARTILLERY_CONFIG}" ]; then
      echo "ERROR: No Artillery config found. Set ARTILLERY_CONFIG=path/to/config.yml"
      exit 1
    fi
    TARGET_ARG=""
    if [ -n "${ARTILLERY_TARGET}" ]; then
      TARGET_ARG="--target ${ARTILLERY_TARGET}"
    fi
    echo "Running Artillery: ${ARTILLERY_CONFIG}"
    # NOTE: `artillery report` was removed in Artillery v2; HTML reports now
    # require a v2-supported path (e.g. artillery-plugin-publish-metrics or an
    # external converter). JSON results remain in artillery-results.json.
    run-logged artillery-reports/artillery.log -- artillery run "${ARTILLERY_CONFIG}" ${TARGET_ARG} --output artillery-reports/artillery-results.json
  - "echo \"Artillery load test complete. Reports available in artillery-reports/\""
env:
  ARTILLERY_CONFIG: ""
  ARTILLERY_TARGET: ""

# ─── Catalog metadata (plugin-ecosystem W0.2) ───
license: Apache-2.0
homepageUrl: https://www.artillery.io
sourceUrl: https://github.com/mwashburn160/pipeline-builder/tree/main/deploy/plugins/testing/artillery
# icon: none — no mark in Simple Icons 16.32.0; no confirmed vendor press-kit permission (monogram; see _icons/SOURCES.md)
# network.egress: none declared — the step contacts no fixed external host
changelog: |
  1.0.0: Initial Official catalog release. Runs as the non-root `plugin` user (uid 1000); every download pinned and digest-verified.
