name: trivy-java
description: Trivy security scanning for Java/Kotlin projects using AWS CDK with CodeBuildStep
keywords:
  - java
  - vulnerability
  - dependency-scan
  - cve
category: security
version: 1.0.0
pluginType: CodeBuildStep
computeType: SMALL
timeout: 15
failureBehavior: warn
secrets: []
primaryOutputDirectory: trivy-reports
dockerfile: Dockerfile
installCommands:
  - test -d trivy-reports || mkdir -p trivy-reports
  - |
    set -e
    if [ -f /opt/trivy/versions/trivy-${TRIVY_VERSION} ]; then
      ln -sf /opt/trivy/versions/trivy-${TRIVY_VERSION} /usr/local/bin/trivy
    else
      curl -fsSL https://github.com/aquasecurity/trivy/releases/download/v${TRIVY_VERSION}/trivy_${TRIVY_VERSION}_Linux-64bit.tar.gz \
        | tar -xz -C /tmp && mv /tmp/trivy /usr/local/bin/trivy
    fi
  - trivy --version
  - |
    set -e
    # CodeBuild runs under /bin/sh (dash); SDKMAN's bash-only `sdk`/`source` fail
    # there (exit 127). Put the requested java on PATH directly — the image installs
    # each version under ${SDKMAN_DIR}/candidates/java/<version>/bin and has the
    # default's `current` on PATH, so an unknown override falls back.
    export PATH="${SDKMAN_DIR}/candidates/java/${JAVA_VERSION}/bin:${PATH}"
    if [ -f pom.xml ]; then mvn dependency:resolve -q; fi
    if [ -f build.gradle ] || [ -f build.gradle.kts ]; then gradle dependencies --quiet; fi
commands:
  - trivy fs --severity ${TRIVY_SEVERITY} --format ${TRIVY_FORMAT} --output trivy-reports/trivy-fs-results.json .
  - trivy config --severity ${TRIVY_SEVERITY} --format ${TRIVY_FORMAT} --output trivy-reports/trivy-config-results.json . || true
  - "echo \"Trivy scan complete. Reports available in trivy-reports/\""
env:
  TRIVY_VERSION: "0.69.3"
  TRIVY_SEVERITY: HIGH,CRITICAL
  TRIVY_FORMAT: json
  # Major-version knob (consistent with the Corretto plugins). The image installs
  # Temurin 25.0.3 via SDKMAN, exposed as `candidates/java/25` by a Dockerfile symlink.
  JAVA_VERSION: "25"
