title: Self-Service CI/CD for AWS
description: Self-service AWS CodePipeline platform — developers ship compliant CI/CD pipelines in minutes via dashboard, CLI, CDK, or AI prompt, while platform teams enforce policy-as-code guardrails, governance, and per-team isolation.
tagline: Self-service CI/CD for AWS

url: "https://mwashburn160.github.io"
baseurl: "/pipeline-builder"

author:
  name: Mark Washburn
  email: mwashburn160@gmail.com

# SEO & Social
twitter:
  username: mwashburn160   # Add your actual Twitter if you have one
  card: summary_large_image

social:
  name: Pipeline Builder
  links:
    - https://github.com/mwashburn160/pipeline-builder
    # Add more if available (LinkedIn, Twitter, etc.)

# SEO Enhancements
keywords: aws codepipeline, aws cicd, self-service cicd, internal developer platform, platform engineering, ai pipeline generator, codepipeline builder, aws codebuild plugins, policy as code, compliance as code, golden paths, multi-tenant cicd, cdk pipelines, devops automation

theme: jekyll-theme-minimal

plugins:
  - jekyll-seo-tag
  - jekyll-sitemap
  - jekyll-feed
  - jekyll-relative-links

github: [metadata]

relative_links:
  enabled: true
  collections: true

# Make sure Jekyll SEO tag can generate proper Open Graph tags
defaults:
  -
    scope:
      path: ""
    values:
      image: /assets/og-image.png   # Create this image (1200x630 recommended)
      author: Mark Washburn

exclude:
  - README.md
  - node_modules/
  - packages/
  - api/
  - platform/
  - frontend/
  - deploy/
  - .projen/
  - projenrc/
  - "*.ts"
  - "*.json"
  - "*.yml"
  - "!_config.yml"
  - pnpm-lock.yaml
  - Gemfile
  - Gemfile.lock
