{
  "name": "add-plugin",
  "scripts": {
    "build": "export PATH=$PATH:`which sam`&& npx sam build",
    "local-api": "export PATH=$PATH:`which sam` && npx sam build && npx sam local start-api",
    "deploy": "export PATH=$PATH:`which sam` && npx sam build && npx sam deploy --stack-name add-plugin-stack"
  },
  "pnpm": {},
  "engines": {
    "node": ">= 22.15.0"
  },
  "version": "0.0.27"
}
