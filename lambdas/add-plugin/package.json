{
  "name": "add-plugin",
  "scripts": {
    "build": "npx sam build",
    "serve-functions": "npx sam build && npx sam local start-api",
    "deploy-functions": "npx sam build && npx sam deploy --stack-name add-plugin-stack"
  },
  "pnpm": {},
  "engines": {
    "node": ">= 22.15.0"
  },
  "version": "0.0.25"
}
